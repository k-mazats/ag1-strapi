import UserBlock from "../UserBlock/UserBlock";
import "./SideBar.css";

const SideBar = () => {
	return (
		<div className="side-bar">
			<UserBlock></UserBlock>
		</div>
	);
};

export default SideBar;
